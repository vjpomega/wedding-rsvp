version: '3'

services:
  postgres:
    image: 'postgres:16'
    environment:
      - POSTGRES_DB=rsvp
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_USER=postgres
    ports:
      - "57745:5432"